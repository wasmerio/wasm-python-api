# WebAssembly Python API

This is a draft, a Work In Progress.

## Design Goals

## Interfaces

The Python API is implemented as a set of abstract classes, that can
be found in the `webassembly/` directory.
